[package]
name = "shared_crawler_api"
version = "0.1.0"
edition = "2021"

[lib]
name = "shared_crawler_api"
path = "src/lib.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
