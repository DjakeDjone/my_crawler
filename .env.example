# Global Port Configuration
# This file contains all port settings for the crawler project
# Copy this file to .env and adjust the values as needed

# API Server (Rust - api service)
API_HOST=127.0.0.1
API_PORT=8000

# Spider/Crawler Server (Rust - spider service)
SPIDER_HOST=127.0.0.1
SPIDER_PORT=8001

# Crawler Settings
MAX_CONCURRENT_REQUESTS=4
MAX_RETRIES=3
RETRY_BASE_DELAY_MS=1000
MAX_CRAWL_DEPTH=10

# Weaviate Vector Database
WEAVIATE_HOST_PORT=8080
WEAVIATE_GRPC_PORT=50051
WEAVIATE_INTERNAL_PORT=8080

# Weaviate Connection URL (used by Rust services)
WEAVIATE_URL=http://localhost:8080

# Ollama (no exposed ports by default, but available for configuration)
# OLLAMA_HOST_PORT=11434
